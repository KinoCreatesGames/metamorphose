package ldtkData;

/**
 * Generates the LDTkProj.hx classes and type definitions
 * for working with the LDTK Haxe API when creating your own
 * game with LDTK tile map editor.
 */
#if !macro
private typedef _Tmp = haxe.macro.MacroType<[ldtk.Project.build('res/maps/metamorphose.ldtk')]>;
#end
